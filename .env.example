# Supabase (proyecto → Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Solo servidor — invitar usuarios / operaciones admin (no exponer al cliente)
# SUPABASE_SERVICE_ROLE_KEY=
